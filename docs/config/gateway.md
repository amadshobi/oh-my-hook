# Gateway Configuration

Configuration options for the local gateway bridge and upstream discovery.

---

## ️ Full Schema

```jsonc
// ~/.config/opencode/omh.jsonc
{
 "gateway": {
 // Enable local gateway bridge and model discovery
 "enabled": true
 }
}
```
